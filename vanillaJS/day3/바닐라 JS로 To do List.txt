! to do list를 만들자 !

* * * 출력 & 등록 * * * 

2) 기존에 localStorge에 저장 되어있는걸 화면에 출력하고
  2-1) 기존에 localStorage에 저장 되어있는걸 불러온다.
  2-2) 만약 null이 아니라면 String -> obj(배열) 전환한다.
  2-3) 그리고 이 배열을 foreach를 이용해서 화면에 출력한다. 이때 기존에 저장되어있는 값들을 다시 배열에 저장해주어야한다. 그래야 이어서 저장할 수 있다.
// 왜냐면 새고하면 배열은 날라가기때문에 

1) 폼에 입력을 하면 이벤트를 발생시킨다. -> 이걸 1번으로 구현함 ㅇㅇ ! 

 1-1 )form에 submit이 발생하면 handle submit을 발생시킴
 1-2 ) hadlesubmit은 input 들어온 값을 저장하고 저장한 값을 화면에 출력한다. 이후 input의 값 초기화
 1-3 ) 이때 들어온 값을 객체에 넣어주고 그걸 배열에 저장하고 그 배열(객체)를 Stirng으로 바꿔서 localStorage에 저장한다.
    // 왜냐하면 localStorage에는 obj를 저장할 수 없기 때문에 


* * * 삭제 기능 * * * 

1) localStorage에서 지우고 저장 
2) html에서 삭제  



* * * 등록 시간 추가 하기 * * * 

//왜 시간이 새로고침한 시간으로 바뀔까? 

2) 새로고침을 하면 기존 localStorage에 저장되어 있는 배열에서 하나 꺼내고 출력 - 배열에 저장 - ls에 저장 ->
다른 하나 꺼내서 배열에 저장 - ls에 저장해서 기존 local과 동일한 내용의 배열을 만들어놓아서 이어서 저장할 수 있게한다.
-> 새로고침하면 localStorage에 기존 값이 저장 되어있으나 새로 입력하면 기존의 localStorage 값은 사라진다 
-> 왜나하면 입력할 때 배열에 넣고 그걸 local로 보내기 때문에 이때 보낸 배열로 local이 바뀌기때문에 
-> 이 과정에서 기존에 저장되어있는 값이 새로 고침한 시간 (new date () ) 으로 변경되어 배열에 저장된다 . 

1) 내가 입력한 값을 화면에 출력하고 그걸 배열과 localStorage에 저장을 한다.


>> Date 객체는 항상 내부적으로 utc 기준으로 저장 하고 출력할 때는 운영체제에서 정의한 표준시간에 맞게 변환한다.  
 그래서 출력은 kst로 나오지만 local 저장할 때는utc 기준으로 저장 되는 것 나는 이 저장된걸 그대로 불러오기에
 utc로 나온다. 





  
  

 